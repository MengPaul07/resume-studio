"""TargetedSelfChecker — deprecated, replaced by _compose_and_self_check."""

# Kept as a stub to avoid breaking imports in case of external references.
# The compose+check pipeline now runs as a single LLM call in turn_runner.py.
