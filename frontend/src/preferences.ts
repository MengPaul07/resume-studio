import type { ResumeGenerationPreferences } from './types';

// Shared default layout preferences used by render/tailor flows.
export const DEFAULT_PREFERENCES: ResumeGenerationPreferences = {
  metadata: {},
};
